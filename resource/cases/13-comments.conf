// ============================================================
// INVALID (unformatted input)
// ============================================================

# This is a hash comment
key1="value1"

# Another hash comment
key2="value2"

# comment indentation in objects
obj={
      # over-indented comment
key3="value3"
}

# blank lines between comments preserved
# first comment

# second comment after blank line
key4="value4"

# comment scope: comment before nested object stays at root level
  // cache settings
dragonfly = {
  remote-cache = {
    host = "localhost"
  }
}

# multiple comments before nested object
// line 1
 // line 2
   // line 3
nested = {
  inner = {
    key = "value"
  }
}

// ============================================================
// VALID (expected formatted output)
// ============================================================

// This is a hash comment
key1 = "value1"

// Another hash comment
key2 = "value2"

// comment indentation in objects
obj = {
  // over-indented comment
  key3 = "value3"
}

// blank lines between comments preserved
// first comment

// second comment after blank line
key4 = "value4"

// comment scope: comment before nested object stays at root level
// cache settings
dragonfly = {
  remote-cache = {
    host = "localhost"
  }
}

// multiple comments before nested object
// line 1
// line 2
// line 3
nested = {
  inner = {
    key = "value"
  }
}
