// ============================================================
// INVALID (unformatted input)
// ============================================================

# nested objects with bad style
server
{
    host = "localhost"
     port = 8080
  ssl
  {
enabled = true
      certificate = "/path/to/cert.pem"
  }
}

// ============================================================
// VALID (expected formatted output)
// ============================================================

// nested objects with bad style
server = {
  host = "localhost"
  port = 8080
  ssl = {
    enabled = true
    certificate = "/path/to/cert.pem"
  }
}
