// ============================================================
// INVALID (unformatted input)
// ============================================================

# dotted paths with bad spacing
server.host="localhost"
server.port:8080
database.connection.timeout =30

// ============================================================
// VALID (expected formatted output)
// ============================================================

// dotted paths with bad spacing
server.host = "localhost"
server.port = 8080
database.connection.timeout = 30
