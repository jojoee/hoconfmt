// ============================================================
// INVALID (unformatted input)
// ============================================================

# concatenation bad spacing
name="World"
greeting="Hello, "${name}"!"
base-path="/app"
config-path=${base-path}"/config"

// ============================================================
// VALID (expected formatted output)
// ============================================================

// concatenation bad spacing
name = "World"
greeting = "Hello, "${name}"!"
base-path = "/app"
config-path = ${base-path}"/config"
