# concatenation bad spacing
name="World"
greeting="Hello, "${name}"!"
base-path="/app"
config-path=${base-path}"/config"

# duration values (number + unit concatenation)
timeout = 60 seconds
delay = 2 s
keepalive = 60s
