// Style fixes test cases

// Fix #1: Duration values combined into single quoted string
timeout = "60 seconds"
delay = "2 s"
keepalive = "60s"

// Fix #2: Comment indentation normalized
obj = {
  // comment at proper indent level
  key = "value"
}

// Fix #5: Space preserved when extending substitution
config = ${base.config} {
  extended = "value"
}

// Fix #6: Blank lines preserved between comments
// first comment

// second comment after blank line
field = "value"
