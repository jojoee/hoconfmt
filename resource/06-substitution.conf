# variable substitution bad spacing
base-path="/app"
config-path=${base-path}"/config"
optional-path=${?CUSTOM_PATH}

# substitution extension pattern (space before object)
extended-config = ${base.config} { key = "value" }
